<?xml version="1.0" encoding="utf-8"?>
<!--
  Android Manifest for Ball Sort Puzzle React Native App
  Optimized for Android API 21+ with all required permissions and configurations
  Includes AdMob, Google Play Games Services, and performance optimizations
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required Permissions -->
    
    <!-- Internet access for AdMob and Google Play Games Services -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Network state for connectivity checks -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Wake lock for preventing sleep during gameplay -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Vibration for haptic feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Google Play Games Services -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    
    <!-- Optional: Write external storage for save games (API < 29) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    
    <!-- Optional: Read external storage for loading assets (API < 29) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    
    <!-- System alert window for overlays (if needed) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Hardware and Software Requirements -->
    
    <!-- Require touchscreen -->
    <uses-feature 
        android:name="android.hardware.touchscreen"
        android:required="true" />
    
    <!-- Optional: Hardware acceleration -->
    <uses-feature 
        android:name="android.hardware.screen.landscape"
        android:required="false" />
    
    <!-- Optional: Gamepad support -->
    <uses-feature 
        android:name="android.hardware.gamepad"
        android:required="false" />
    
    <!-- Require OpenGL ES 2.0 for graphics -->
    <uses-feature 
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false"
        android:requestLegacyExternalStorage="false"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:supportsRtl="true"
        tools:targetApi="31">

        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}" />
        
        <!-- Google Play Games Services App ID -->
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="@string/google_play_games_app_id" />
        
        <!-- Google Play Games Version -->
        <meta-data 
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait"
            android:theme="@style/LaunchTheme">
            
            <!-- Main app launcher intent -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Deep link support for sharing scores -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ballsortpuzzle" />
            </intent-filter>
        </activity>

        <!-- Google Play Games Services Sign-In Activity -->
        <activity
            android:name="com.google.android.gms.games.ui.signin.SignInActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- AdMob Ad Activities -->
        <activity
            android:name="com.google.android.gms.ads.AdActivity"
            android:exported="false"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:theme="@android:style/Theme.Translucent" />

        <!-- React Native Development Activities (Debug only) -->
        <activity 
            android:name="com.facebook.react.devsupport.DevSettingsActivity"
            android:exported="false" />

        <!-- File Provider for sharing content -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Backup Agent for game progress -->
        <service
            android:name=".backup.GameBackupAgent"
            android:exported="false" />

        <!-- Work Manager initialization -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Game state sync service -->
        <service
            android:name=".services.GameSyncService"
            android:exported="false"
            android:enabled="true" />

        <!-- Notification channels for Android 8.0+ -->
        <receiver
            android:name=".notifications.NotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

    </application>

    <!-- Queries declaration for Android 11+ package visibility -->
    <queries>
        <!-- Google Play Games app -->
        <package android:name="com.google.android.play.games" />
        
        <!-- Google Play Store -->
        <package android:name="com.android.vending" />
        
        <!-- Web browsers for external links -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        
        <!-- Email apps for feedback -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

</manifest>